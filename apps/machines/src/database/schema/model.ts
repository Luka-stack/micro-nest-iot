// export const modelsRelations = relations(models, ({ one }) => ({
//   type: one(types, {
//     fields: [models.typeId],
//     references: [types.id],
//   }),
//   producent: one(producents, {
//     fields: [models.producentId],
//     references: [producents.id],
//   }),
// }));
